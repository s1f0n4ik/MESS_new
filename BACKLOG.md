# BACKLOG

Что осталось сделать до финальной цели. Сгруппировано по эпикам. Внутри эпиков — по приоритету.
Статусы: [ ] todo · [~] в работе · [x] done · [!] тех-долг/требует решения

## EPIC A — Транспорт и сеть
- [x] Перевести SSE → WebSocket (двусторонний). Клиент шлёт identify (role, hostName). `/ws` проксируется vite, end-to-end подтверждён.
- [x] Выпилить SSE (`/api/stream`) либо явно решить роли двух транспортов — сейчас живут оба.
- [x] Починить «дёргающиеся» WS-соединения (open→closed→open): корректный teardown в useEffect, проверка StrictMode, дебаунс reconnect.
- [ ] identify расширить: localIps, wantsServer.
- [x] Координатор ведёт список подключённых устройств (connectedDevices) + lastSeen.
- [ ] Zeroconf/mDNS discovery координатора + ручной ввод IP (serverHost) как fallback.
- [x] Роль клиента (pc1..pc4) как настройка, а не хардкод `?role=pc1`.
- [~] Reconnect-логика на клиенте, статус online/offline.

## EPIC B — Логика сценария и волн (ядро, самое тонкое)
- [x] Заменить простой круг `launch` на модель волн: waveIndex (1..4), waveSettled (открыты РОВНО pc1..pcN), popupEpoch, popupPage.
- [x] Продвижение по ноте launch: волна 1→2→3→4→закрыть (advance_wave).
- [x] forceOpenAll — открыть/закрыть окна на всех ПК (+ restoreAfterForce).
- [x] Разнести сброс: reset_scenario (сохраняет клики/перевороты) и hard_reset.
- [x] Выбор источника PDF в открытом окне: своя pcX.pdf vs pcN.pdf текущей волны (по waveSettled). → Slice 6
- [~] Порог кликов (17) → старт сценария + lock роли — есть, старт всегда с pc1; под волны подвязано.
- [ ] Настройка «n секунд до открытия ПК-1 в конце 1-го круга».
- [ ] Первый круг — «маятник» pc1→2→3→4→3→2→1 (по одному окну, до MIDI-ноты), отличен от накопительных волн 2–4. Уточнить модель.
- [!] Модель закладок окна (addPdfTab из pdf.html) — ОТДЕЛЬНАЯ модель, не резолв
      currentPopupPayload (тот даёт ровно один файл на окно). Решить в EPIC C, не
      смешивать с автоматикой волн. pdfWindowsByRole на координаторе НЕ нужен, пока нет
      per-окно закладок — резолв per-client по myRole закрывает основной сценарий.
- [ ] Таймеры на координаторе: 2 минуты между кругами 2–4; настраиваемые n сек для возврата pc1 в конце 1-го круга.

## EPIC C — Управление окнами (Tauri, нативно)
- [ ] PDF-окно как нативное Tauri WebviewWindow: размер 1968×1392, без заголовка/декораций, позиционирование по центру.
- [ ] Открыть/скрыть/сфокусировать/переместить PDF-окно из состояния.
- [ ] Перезагрузка PDF по mtime файла (cache-buster).
- [ ] Вкладки внутри PDF-окна (несколько pdf).
- [ ] «Свернуть все окна Windows» → нативно через Tauri (вместо Shell.Application MinimizeAll). На прочих ОС — эквивалент/no-op.
- [ ] Управление кэшем профиля просмотрщика (просмотр размера + очистка).

## EPIC D — Главная сцена
- [ ] Жёсткая пиксельная сетка 3440×1440, CARD_POSITIONS (2 ряда по 4).
- [ ] Реальные картинки front/back открыток вместо номеров.
- [ ] Антивандал: блок F5/F12/контекст-меню/Ctrl+колесо/pinch/auxclick/drag/select.
- [ ] Хоткеи: M/Ь меню, E/У редактирование, Q/Й перевернуть все, F fullscreen, Ctrl+Shift+R сброс.
- [ ] Меню только при ?admin=1.

## EPIC E — Аудио
- [ ] 16 типов звуков на ПК, проигрывание по клику, прерывание предыдущего.
- [ ] Keep-alive аудио-loop (разблокировка автоплея).
- [ ] Включение/выключение аудио в настройках.

## EPIC F — MIDI
- [x] Выбор конкретного MIDI input/output в UI, с дефолтом `PC-10` при наличии.
- [x] Слушать только выбранный MIDI input, а не все inputs сразу.
- [x] Фильтр по MIDI-каналу (`channel = 2` по legacy-конфигу).
- [x] Предзаполнение legacy-маппинга:
  - launch=60
  - open: pc1=61, pc2=63, pc3=65, pc4=67
  - close: pc1=62, pc2=64, pc3=66, pc4=68
  - minimizeAll=69
  - output=72
  - velocity=100
  - duration=180
- [~] Дедупликация входящих MIDI-сообщений (локально есть, но по полевому логу видны повторы входа; дочистить/подтвердить на выбранном input).
- [x] Привязка MIDI note -> sendAction(action, payload).
- [x] Поддержка legacy-экшенов:
  - open_role_popup(pc1..pc4)
  - close_role_popup(pc1..pc4)
  - launch
  - toggle_force_open_all
  - reset_scenario
  - minimize_all_windows
- [~] Тест-кнопка MIDI output через выбранный output (`PC-10`) — реализована, нужна полевая проверка.

## EPIC G — Окно экфрасисов (popup 2052×966)
- [ ] Разворот: 2 страницы (картинка + side-text + bottom-text), 8 разворотов, навигация.
- [ ] Режим редактирования (E/У, только на сервере), хранение текстов локально.
- [ ] Синхронизация текущей страницы между окнами.
- [ ] Анимации открытия/закрытия.

## EPIC H — Настройки и персист
- [ ] local-settings.json на каждом ПК (роль, сеть, MIDI, аудио, PDF-маппинг).
- [ ] global-settings.json на координаторе (общие параметры сценария).
- [ ] Сохранение/сброс настроек, persist текстов экфрасисов.

## EPIC I — Админка
- [ ] Вкладки по 4 ПК: вкл/выкл, статус, логи каждого.
- [ ] PDF-менеджер: список, превью, привязка к ПК/волне.
- [ ] MIDI-мониторинг, кэш-менеджер, сброс сценария/настроек.

## EPIC J — Сборка и эксплуатация
- [ ] Python sidecar внутри Tauri (упаковка backend).
- [ ] Прод-сборка frontend в Tauri.
- [ ] Кросс-платформенная проверка (Windows основная, остальное best-effort).

# BACKLOG

## ✅ DONE
- Slice 1–5: WS-хаб, состояние, клики, порог 17, ядро волн, force_open_all, reset/hard_reset.
- Slice 6: PDF-резолвер (pc{wave}.pdf на проходе, домашний pcX.pdf в осевшем), pdfWindow в state, pdfWindowsByRole.
- EPIC A1: роль из настроек (role.js: localStorage -> ?role= -> pc1), единый источник истины,
  селект смены роли с reload, убран дублирующий getMyRole() из PdfWindowLayer (роль = проп myRole).
- EPIC A2: connectedDevices с lastSeenAt; ping двигает lastSeen; фоновый device_sweeper
  помечает online:false при простое > DEVICE_STALE_SECONDS (30s), проверка каждые 5s;
  переход на FastAPI lifespan для запуска/останова задачи; UI показывает "Ns назад".

## 🔜 NEXT
### Slice 7 — «Маятник» (волны 2–4) + серверные таймеры (EPIC B, остаток)

ПРОДУКТОВАЯ СПЕКА (со слов заказчика, требует сверки с ТЗ):
В каждый момент волны открыто РОВНО ОДНО окно; оно "пробегает" по ПК
вперёд и назад: pc1→pc2→...→pcN→...→pc2→pc1.
По итогам волны N "оседает" домашняя раскладка: pc1=pdf1, pc2=pdf2, ... pcN=pdfN.
Во время пробега ВСЕ показывают pdf текущей волны (бегущее окно несёт pdfN);
после оседания каждый ПК возвращается к своему домашнему pdf.
=> Это объясняет резолвер Slice 6 и поле waveSettled (наполняет их смыслом).
=> Это НОВАЯ логика поверх эталона server.js (там launch лишь двигал счётчик,
   накопительно открывая pc1..pcN; пробега/маятника не было).

### Slice 7 — Первый круг «маятник» (MIDI) + круги 2–4 (таймер 2 мин) ✅ ВОПРОС ИСТОЧНИКА ПРОДВИЖЕНИЯ ЗАКРЫТ (цитата ТЗ приложена в PROGRESS). Ответ: источников ДВА, разнесены по фазам — это не противоречие, а две механики. - Первый круг (pendulum): pc1→2→3→4→3→2→1, 7 шагов, продвижение КАЖДЫМ приходом MIDI-ноты launch. Каждый шаг: открыто РОВНО одно окно у роли шага. - Круги 2–4 (dwell): продвижение СЕРВЕРНЫМ ТАЙМЕРОМ раз в 2 минуты, накопительно (после круга N открыты pc1..pcN). - Финал: 4-й круг осел → ждём ту же MIDI-ноту на pc4 → close_scenario → откат. КЛЮЧЕВОЕ АРХИТЕКТУРНОЕ СЛЕДСТВИЕ (НЕ игнорировать): waveIndex и «маятник» — РАЗНЫЕ оси состояния. Текущий advance_wave (из server.js) описывает ТОЛЬКО dwell-круги 2–4. Маятник он не описывает — нужен отдельный pendulumStep (0..6). Не натягивать одно на другое. МОДЕЛЬ СОСТОЯНИЯ (план): scenario.phase ∈ { 'pendulum', 'dwell', 'final_hold' } (огрубляет текущее manual_midi/force_open_all) scenario.pendulumStep: int 0..6 (только в фазе pendulum) PENDULUM_ROUTE = [pc1,pc2,pc3,pc4,pc3,pc2,pc1] scenario.waveIndex: 1..4 (только в фазе dwell, как сейчас) ТЕХ-ЗАДАЧИ: - [ ] phase-машина pendulum→dwell→final_hold + переходы. - [ ] MIDI-нота launch в фазе pendulum: pendulumStep += 1, пересобрать openRoles (одно окно). - [ ] На step==6 (осели pc1): phase='dwell', waveIndex=1, взвести таймер 2 мин. - [ ] Серверный таймер 2 мин (через lifespan, как sweeper): waveIndex += 1, dwell-накопление. - [ ] waveIndex==4 осел → phase='final_hold', таймер не перевзводить. - [ ] MIDI-нота на pc4 в final_hold → close_scenario → откат. - [ ] resolvePdfWindow: ОТДЕЛЬНАЯ ветка для pendulum (sourceRole = роль текущего шага, НЕ pc{waveIndex}). ОТКРЫТЫЕ ПОДВОПРОСЫ (не блокируют старт, уточнить по ходу): - «n сек до открытия pc1 в конце 1-го круга» (из формы панели) — это пауза между концом маятника и стартом dwell? Похоже да. Параметр настраиваемый. - Старт сценария по 17 кликам сейчас сразу ставит openRoles[pc1]=true. В новой модели это должно быть началом pendulum (step=0), а не отдельным состоянием. 

ТЕХ-ЗАДАЧИ Slice 7 (после снятия вопроса):
- модель прохода: индекс позиции в маятнике + направление (fwd/back);
- тайминги шагов пробега (сколько окно держится на каждом ПК);
- интеграция с lifespan-таймером (мы его уже умеем) ИЛИ обработчик MIDI-ноты;
- сверка с резолвером: на проходе pdf = pdfN, в settled = домашний.

## 🧊 LATER
- Десктоп-сборка (Electron/Tauri) — намеренно отложено в конец.
- MIDI-out (есть в эталоне app.js: maybeSendMidiOut на scenario_started/click_threshold).
- keepAlive-audio loop (из app.js) — нужен ли в новой архитектуре?


- [x] Slice 7A — phase-machine (`pendulum` / `dwell` / `final_hold`) заведена, но переход `pendulum -> dwell` пока схлопнут; нужен явный наблюдаемый финальный шаг `pendulumStep=6`.
- [~] Slice 7B — resolvePdfWindow для `pendulum`: источник PDF = роль шага маятника, а не `pc{waveIndex}`.



